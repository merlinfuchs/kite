export default function MessageComponentButton({
  rowIndex,
  rowId,
  compIndex,
  compId,
}: {
  rowIndex: number;
  rowId: number;
  compIndex: number;
  compId: number;
}) {
  return <div></div>;
}
